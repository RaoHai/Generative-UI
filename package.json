{
  "name": "generative-ui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8080",
    "server:dev": "cd server && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8080",
    "server:venv": "cd server && .\\venv\\Scripts\\python -m uvicorn main:app --reload --host 0.0.0.0 --port 8080"
  },
  "engines": {
    "node": "^18 || >=20"
  },
  "repository": "https://github.com/petercat-ai/petercat.git",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}